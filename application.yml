spring:
  cloud:
    stream:
      bindings:
        audit-out-0:
          destination: banking.audit.logs
      kafka:
        binder:
          brokers:
            - ${KAFKA_URL:localhost:9092}
          connection-timeout: 5